// const nomes = ['Whinds', 'Freeway', 'Teste', 'Maria'];

// nomes.forEach((nome, i) => {
//   console.log('[forEach]', nome, i);
// });

const nomes = ['Whinds', 'Freeway', 'Teste', 'Maria'];

nomes.map((nome, i) => {
  console.log(nome, i);
});
